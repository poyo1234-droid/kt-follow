# 引き継ぎ_未処理（FAフォロー業務）

最終更新: 2026-09-10（夜）

**このファイルには到達点と残タスクだけを書く。**経緯や詳しい数字は一次資料（下記）に置く。
古くなった記述は消す。

---

## 現在地

メーカーID=2で全項目の突合が完了。差分の原因はすべて特定済み。
2026-09-10は「価格系3列の保持先」の検討から商品マスタの管理方針を議論し、
「商品マスタに3項目を追加してgoods_csから差分更新する」案をいったん決めたが、**同日白紙に戻した**（煩雑・間違いが起きそう・更新時に問題がありそう）。
検討の記録と、有効な事実（商品展開IDはキーに使える／販売日は更新日にならない／商品マスタを使う仕組みの一覧）は一次資料に残してある。
**価格系3列の保持先は未決に戻った。**
その後「1ファイル＝1アプリ（goods_csアプリ・メーカー在庫切れSKUアプリを新設）」の方向で検討中 → `claude/商品マスタのあり方_検討書_20260910.md`。

エンジニアへの差し戻し（6件）・担当者への確認（3論点）は送信済み・回答待ち。

**リポジトリ: kt-follow**
`T:\10_業務資料\ファンアライブ\250829サーバー更新\260907-フォローデータ`

---

## 次にやること

### 1. 商品マスタのあり方の検討を続ける（検討書の「7. 未決」）
- 次の一手: テスト用アプリで1万行を新規追加し取込時間を測る／登録日で絞ったgoods_csの見出しを確認

### 1-2. 価格系3列の保持先（上の検討次第）
- 維持する前提: 商品マスタのCS別品番は上書きしない／同じ商品を2重に出力しない／最優先は旧システム（Win10）からの脱却
- 担当者の「ご確認1」（3列の要否）の回答を待つ。削除でよければこの論点は消える

### 2. 担当者への確認の見直し
送信済みの「ご確認1」（3列の要否）は「goods_cs 1GBの取込が負担」という前提で書いている。
回答を見て必要なら補足する。未送信の「★追加確認」（軽量抽出ファイル）の要否も回答次第。

### 3. 自分の作業（2件・未着手）
- CSV分割ツール `csv_splitter_gui.py` のバグ修正＋在庫アプリ再取込
- テンプレートのJANコード列の物理行数を増やす（保険的対応）

### 4. エンジニア関係（回答待ち・追加確認は未送信）
差し戻し6件は回答待ち。salegoodsデッドコードの追加確認は未送信（文面は差し戻し資料にあり）。
2番「通常▲」ルールは、3列を残す場合に必要。

### 5. 再検証・他メーカー確認
修正後に `検証スクリプト\フォローリスト突合.py 2` で不一致0を確認。他メーカーは出力が揃ってから。

---

## 注意点（間違えやすいところ）

- **2026-09-10、Coworkのファイル連携が不調。**PC上のシェルは全フォルダ（ローカル含む）でマウント失敗、
  Tドライブはファイルの取り込みも不可（一覧のみ可）。ローカル（`C:\claude-test`）からの取り込みは可。
  9/9までは正常だった。アプリ・Windows・PC再起動でも直らず（アプリ 1.49585.0）。回避策はローカル経由か、スクリプトをユーザーがWindowsで実行
- kintone: `https://zrqf50naoiwx.cybozu.com`。商品マスタ＝アプリ51。ChromeからREST API（GETのみ）で設定を読める
- goods_csの「販売開始日」はセール適用で書き換わるが、セール終了では変わらない → 更新日の代わりにならない
- goods_csのキーは商品展開ID（CS別品番はZOZO上で変わる）。`使ったデータ\goods_cs_正.csv` は加工済み（ID・価格に「.0」、見出し変更、通常▲付与、在庫切れSKU除去）
- **旧システムの入力は `使ったデータ\`、キントーンの取込元は `元データ-kintone取込\`。**
- **セール業務とフォロー業務で旧システムの絞り込みが違う**（フォローは在庫0も出力）
- 列見出しは改行を含む（`在庫\n数量` など）
- エンジニアへの差し戻しは、下書き8項目のうち実送信は■1,2,3,4,5,7の6項目
- **`claude/` 側の更新はTドライブ側に自動反映されない。**手動同期が必要

---

## 一次資料

同内容がプロジェクトの `claude/` 配下にある。**必要になったときだけ読む。**

| ファイル | いつ読むか |
|---|---|
| `claude/商品マスタのあり方_検討書_20260910.md` | 商品マスタの管理方針の検討を続けるとき（最新） |
| `claude/商品マスタ差分更新_方針メモ_20260910.md` | 白紙撤回した案の記録。同じ案を繰り返さないために参照 |
| `claude/goods_cs-2時点比較_20260910.md` | 販売日・商品展開IDの検証根拠が要るとき |
| `claude/商品マスタ参照箇所_20260910.md` | 商品マスタを使う仕組み（アプリ・VBA・Python）を確認するとき |
| `claude/followlist-fix-spec-v0.1.md`（＝`仕様書_フォローリスト生成ロジックの修正.md`） | フォローリストの修正を進めるとき。冒頭の「0. タスク一覧」 |
| `claude/followlist-comparison-maker2-20260909.md`（＝`突合結果_20260909_メーカー2.md`） | 根拠・数字が要るときだけ（長い） |
| `claude/担当者への確認事項_フォローリスト移行_20260909.md` | 担当者メールの内容 |
| `claude/エンジニアへの差し戻し_フォローリスト_20260909.md` | エンジニアメールの内容・実送信履歴 |
| `claude/kintone-mismatch-20260907.md` / `claude/csv-comparison-20260907.md` | 取込の未読み込み行・CSV比較を追うとき |

検証スクリプト `goods_cs_diff_check.py`（goods_cs 2時点比較）は今回新規作成。
