# CLAUDE.md

このフォルダで作業する際の前提情報。**このリポジトリ固有の情報だけ**をここに書く。

**作業の共通ルールは `cowork-working-rules` スキルにある。**セッションの始め方、
読み込みを増やさない調べ方、`device_bash` の制約、git 運用、成果物の扱いはそちらを見る。
このファイルには重複して書かない。

**git リポジトリ本体はこのフォルダ（`260907-フォローデータ\`）。**origin は
`poyo1234-droid/kt-follow`。NAS 上（`T:` = `\\dop\ドキュメント`）にある。

## セッションを始めるとき

**まず引き継ぎを読む。**現在地・残タスク・注意点がそこにまとまっている。
場所は環境によって違う。**中身が新しいのはリポジトリ側（git管理下）。**

| 環境 | 読むファイル |
|---|---|
| Windows（Tドライブ・このリポジトリ） | **`ドキュメント\引き継ぎ_未処理.md`** |
| Cowork（プロジェクト側） | `claude\引き継ぎ_未処理.md`（**所在案内のみ。中身は持たせない**） |

`claude\` 側は所在案内と、数字を含まない現在地の要点だけを置く。中身の二重管理はしない。
食い違ったら `ドキュメント\` 側を正とする。
プロジェクト側の各文書とリポジトリ側の対応は `claude\00_このフォルダについて.md` にある。

このリポジトリには、フォロー業務の突合とは別に
**ZOZOからのgoods_cs自動ダウンロード（`ZOZO自動ダウンロード\`）**がある。
そちらを触るときは `ZOZO自動ダウンロード\仕様書_ZOZO自動ダウンロード.md` を読む。

作業を進めるときは `ドキュメント\仕様書_フォローリスト生成ロジックの修正.md` の
「0. タスク一覧」を見る。突合の根拠が必要になったときだけ
`ドキュメント\突合結果_20260909_メーカー2.md` を読む（長いので普段は読まない）。

## プロジェクト概要

ZOZOTOWN のフォロー業務で使う「フォローリスト」の生成を、**旧システム（SQL基幹）から
キントーンへ移行する**にあたり、両者の出力が一致するかを突き合わせている。

- **フォローリスト**はメーカー様ごとに1ファイル出力する Excel。在庫・販売実績を提示して
  「フォロー希望数（ファンアライブ記入）」「フォロー可能数（メーカー記入）」をやりとりする
- 生成はキントーンのカスタマイズJS（`JS\260909-フォローmain.js.txt`）が行う。
  Google Drive 上のテンプレートをメーカー別にコピー→値を流し込み→xlsx 化して
  キントーンへ添付する
- **ここでの「旧システム」は SQL をメインにした基幹システム。**セール業務の
  `sale処理-vNN.xlsm` とは別物

## 現在の状況

**現在地は `ドキュメント\引き継ぎ_未処理.md` を見ること。**
ここには経緯を残さない（数字が古くなって誤読のもとになる）。到達点だけ書いておく。

- メーカーID=2（スタンレーインターナショナル様）で全項目の突合が完了。
  **「在庫 ＞ 在庫分析 ＞ 商品マスタ」のフォールバック＋0埋め＋▲ルールで、
  旧データを12項目すべて不一致0で再現できることを実データで確認済み**
- 修正方針は「販売価格・価格タイプ・販売タイプの扱い」を除いて確定。
  エンジニアへの差し戻し8件・担当者への確認3件はメール文面まで作成済み

## データの置き場所

`0907フォローデータ提出用\0907フォローデータ提出用\` 配下。
**巨大なため git では追跡していない**（.gitignore）。

| フォルダ | 内容 |
|---|---|
| `元データ\` | ZOZO から直接ダウンロードした生データ（goods_cs.csv 約1GB / 20260907.csv / S20260907.csv） |
| `元データ-kintone取込\` | 上記＋分割ファイル。**キントーンへ取り込んだのはこちら** |
| `使ったデータ\` | **旧システムが使った入力**。goods_cs_正.csv は在庫切れSKUを除去済みで60,121行少ない |
| `フォローリスト20260907\` | **旧システムの出力**（突合の正） |
| `フォローリスト20260907加工済み\` | 同上。メーカー2では元と完全一致 |
| `フォローリスト-kintone出力\` | キントーンの出力。現在メーカー2の1ファイルのみ |

**旧システムの入力は `使ったデータ\`、キントーンの取込元は `元データ-kintone取込\`。**
ここを取り違えると突合の結論を誤る（実際に一度誤った）。

## 注意

- キントーンを叩くスクリプトは Windows 側で実行する。Cowork のシェルは cybozu.com に出られない
- `使ったデータ\goods_cs_正.csv` は価格が `"1000.0"` 形式で格納されている。
  突合では**数値として比較する**こと（文字列比較だと全件不一致になる）
- 商品名に `"` を含む商品が20件ある（BACK OFFICE の登録名にダブルクォートが入っている）。
  `csv_splitter_gui.py` がこれを2行に割っていたため、在庫アプリが138,129件と20件多い（差し戻し6）。
  **ツールは2026-09-14に修正済み。リポジトリ内の正は `ツール\csv_splitter_gui.py`**
  （旧 `元データ-kintone取込\` 配下のものは `_to_delete_20260914\` へ退避）。
  **在庫アプリの再取込はまだ**
- 各シートの列見出しは改行を含む（`在庫\n数量` など）。スクリプトで列名を指定するときは
  改行込みで書く
